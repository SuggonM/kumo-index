# https://github.com/orgs/community/discussions/16107#discussioncomment-8786272
name: update version-list branch

on:
  schedule:
    - cron: 0 0 * * *

permissions:
  contents: write

jobs:
  run-daily:
    uses: SuggonM/kumo-index/.github/workflows/daily-fetch.yml@version-list
